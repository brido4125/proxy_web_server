#include "cache.h"

CacheList * initCache(){
    CacheList* return_p = NULL;
    return_p = (CacheList*)Malloc(sizeof (CacheList));
    return return_p;
}

void insertCacheNode(CacheList* list,char* ){

}